services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: pass123
      POSTGRES_DB: ragen-auth
    ports:
      - "5433:5432"
    volumes:
      - ragen_auth_pgdata:/var/lib/postgresql/data

volumes:
  ragen_auth_pgdata:
